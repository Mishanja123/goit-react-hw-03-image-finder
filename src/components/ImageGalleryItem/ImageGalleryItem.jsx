import PropTypes from "prop-types";

export const ImageGalleryItem = ({}) => {
    return(
        <li className={galleryItem}>
            <img className={galleryImg} src="" alt="" />
        </li>
    )
}
ImageGalleryItem.propTypes = {

}