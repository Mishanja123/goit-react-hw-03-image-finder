import PropTypes from "prop-types";

export const Modal = ({}) => {
    return(
        <div className={overlay}>
            <div className={modal}>
                <img className={modalImg} src="" alt="" />
            </div>
        </div>
    )
}
Modal.propTypes = {

}